% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AL.R
\name{AL}
\alias{AL}
\title{Alpha Lattice Design Analysis}
\usage{
AL(
  treatments,
  block_size,
  reps = 2,
  data = NULL,
  traits = NULL,
  normality = "shapiro",
  variance = "bartlett",
  mean_sep = "lsd",
  seed = NULL
)
}
\arguments{
\item{treatments}{Either a vector of treatment names (character) or a numeric
value specifying the number of treatments (e.g., 10). If numeric, treatments
are automatically named as "T1", "T2", etc.}

\item{block_size}{Number of plots per block.}

\item{reps}{Number of replicates. Default is 2.}

\item{data}{A data frame containing experimental data. Must include columns
for replication (REP), block (BLOCK), and genotype/treatment (GENOTYPE).
If NULL, the function generates a design layout only.}

\item{traits}{A character vector of column names in \code{data} representing the traits
to analyze. Required if \code{data} is provided.}

\item{normality}{A character string specifying the normality test to perform on
residuals. Options: "shapiro", "ad" (Anderson-Darling), "lillie" (Lilliefors),
or "all" for all tests. Default is "shapiro".}

\item{variance}{A character string specifying the test for homogeneity of variance.
Options: "levene", "bartlett", "fligner", or "all". Default is "bartlett".}

\item{mean_sep}{A character string specifying the mean separation method. Options:
"lsd" (Least Significant Difference), "tukey" (Tukey HSD), "duncan" (Duncan test),
"snk" (Student-Newman-Keuls), or "all". Default is "lsd".}

\item{seed}{Optional numeric value for reproducible randomization.}
}
\value{
If \code{data} is NULL, returns a list with the design layout:
\item{Design}{A data frame containing REP, BLOCK, PLOT, and GENOTYPE.}

If \code{data} is provided, returns a list of results for each trait:
\itemize{
\item \code{Model}: Mixed model object (nlme::lme)
\item \code{VarComp}: Variance components
\item \code{ANOVA}: ANOVA table
\item \code{CV}: Coefficient of variation (\%)
\item \code{Normality}: Normality test results
\item \code{Variance}: Homogeneity of variance test results
\item \code{Means}: Mean values per treatment
\item \code{Groups}: Treatment groupings from mean separation
}
}
\description{
This function generates an Alpha Lattice (incomplete block) design layout
for field experiments or analyzes existing data using mixed models, ANOVA,
residual analysis, normality tests, variance homogeneity tests, and mean separation.
}
\details{
\itemize{
\item If \code{data} is NULL, the function generates a randomized Alpha Lattice design
layout suitable for field experiments.
\item If \code{data} is provided, the function:
\enumerate{
\item Fits a linear mixed model using \code{nlme::lme} with treatments as fixed effects
and replicates/blocks as random effects.
\item Computes variance components.
\item Performs ANOVA for treatment effects.
\item Computes residuals and performs normality tests.
\item Performs homogeneity of variance tests.
\item Computes coefficient of variation (CV).
\item Conducts mean separation according to the selected method(s).
}
}
}
\examples{
# Generate a design layout for 12 treatments in blocks of 4 with 3 replicates
AL(treatments = 12, block_size = 4, reps = 3)

# Analyze existing data
# Assuming 'mydata' has columns REP, BLOCK, GENOTYPE, Yield, Height
AL(treatments = NULL, block_size = 4, data = mydata, traits = c("Yield", "Height"),
   normality = "all", variance = "all", mean_sep = "all")

}
